# INICIO

Librerias Utilizadas
```
pip install numpy pillow pycuda
pip install pycuda flask
```
# EJECUCION

Ejecutamos la aplicacion en el entorno que se haya preparado junto a las librerias. 
 * `Running on all addresses (0.0.0.0)`
 * `Running on`: `http://127.0.0.1:5000`
 * `Running on`:`http://192.168.1.101:5000`
